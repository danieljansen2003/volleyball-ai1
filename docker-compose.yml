services:
  web:
    build: ./apps/web
    ports:
      - "3000:3000"
    environment:
      - AI_WORKER_URL=http://ai-worker:9000
  ai-worker:
    build: ./apps/ai-worker
    ports:
      - "9000:9000"
    environment:
      - MAX_VIDEO_DOWNLOAD_MB=2048
